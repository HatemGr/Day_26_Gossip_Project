<h1> Connexion à ton compte :</h1>

<%# ALERT PART %>
<% flash.any? %>
  <% flash.each do |color,msg|%>
  <div class="alert alert-<%=color%> alert-dismissible fade show" role="alert">
  <strong><%=msg%></strong>
  <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
</div>
<% end %>
<%# ALERT PART %>

<%= form_for :session, url: sessions_path do |f|%>
  <label>Email</label>
  <%= f.text_field :email, class: 'form-control mb-3'%>
  <label>Mot de passe :</label>
  <%= f.text_field :password, class: 'form-control', type:'password'%>
  <div class="container-fluid text-center">
  <%= f.label "Se souvenir de moi" %> 
    <%= f.check_box :remember , class:"form-check-input"%> <br />
  
  <button type="submit" class="btn btn-dark mt-4 mb-5 px-5 py-3">Connexion</button>
  </div>
<%end%>