
<%# ALERT PART %>
<% if flash.any? %>
    <div class="alert alert-<%=flash.keys.first%> alert-dismissible fade show" role="alert">
      <strong><%=flash[flash.keys.first]%></strong><br/>
      <% unless flash[:errors].nil? %>
      <%=flash[:errors].html_safe%>
      <% end %>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div> 
<% end %>
<%# ALERT PART %>

<%= form_for @user, url: users_path do |f| %>
  <label>Prénom :</label>
  <%= f.text_field :first_name, class: 'form-control'%>
  <label>Nom :</label>
  <%= f.text_field :last_name, class: 'form-control'%>
  <label>Ton age :</label>
  <%= f.text_field :age, class: 'form-control'%>
  <label>Ta ville :</label>
  <%= select_tag "city", options_from_collection_for_select(@cities, "id", "name"),class:"form-select"%>
  <label>Email</label>
  <%= f.text_field :email, class: 'form-control'%>
  <label>Mot de passe :</label>
  <%= f.text_field :password, class: 'form-control', type:'password'%>
  <label>Confirmer mot de passe :</label>
  <%= f.text_field :password_confirmation, class: 'form-control', type:'password'%>
  <label>Une petite description de toi ?</label>
  <%= f.text_area :description, class: 'form-control'%>
  <button type="submit" class="btn btn-primary">Creer un compte</button>
<%end%>
