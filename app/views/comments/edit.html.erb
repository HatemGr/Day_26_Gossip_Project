<h1 class="mb-4">Modification</h1>
<p>Que veux-tu changer sur ce commentaire ? ?</p>


 <%=form_for @comment, url: gossip_comment_path(@gossip,@comment), method: :patch do |f|%>
  <div class="form-group my-3">
    <label>Contenu du commentaire: </label>
    <%= f.text_area :content, class: 'form-control', value:@comment.content%>
  </div>
  <button type="submit" class="btn btn-primary">Editer ce commentaire</button>
<%end%>

<%=button_to "Supprimer ce commentaire", gossip_comment_path(
    @gossip.id,@comment.id), method: :delete, class:"btn btn-danger mt-2" %>